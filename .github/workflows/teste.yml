name: End-to-end tests 🧪
on:
  push:
    branches:
      - main

jobs:
  playwright-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: npm ci

      - name: Playwright run
        uses: microsoft/playwright-github-action@v1
        with:
          browserType: chromium
          installBrowser: true
          scriptPath: path/to/your/test/script.js
